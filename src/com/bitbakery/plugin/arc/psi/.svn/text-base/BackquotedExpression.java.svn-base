package com.bitbakery.plugin.arc.psi;

import org.jetbrains.annotations.NotNull;
import com.intellij.lang.ASTNode;

/**
 * e.g., `(1 2 ,x x)  or else (quasi-quote (1 2 
 */
public class BackquotedExpression extends Expression {
    public BackquotedExpression(@NotNull final ASTNode node) {
        super(node);
    }
}
