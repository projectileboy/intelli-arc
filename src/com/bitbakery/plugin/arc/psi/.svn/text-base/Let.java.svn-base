package com.bitbakery.plugin.arc.psi;

import com.intellij.lang.ASTNode;

/**
 * PSI element for Arc "let" blocks
 */
public class Let extends ArcElement {
    public Let(ASTNode node) {
        super(node);
    }
}